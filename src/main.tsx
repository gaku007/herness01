import React from 'react'
import ReactDOM from 'react-dom/client'
import HappyApp from './happy_output.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HappyApp />
  </React.StrictMode>,
)
